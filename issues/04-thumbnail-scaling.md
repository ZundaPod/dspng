# Issue: 图层预览图不随行高缩放

## 描述
调整行高 slider 时，图层列表各行高度变化，但缩略图尺寸不变，导致
缩略图要么过小、要么溢出。

## 修复方案
1. 行高变化时重新生成所有缩略图（以行高为基准确定缩略图尺寸）
2. 缩略图缓存失效 + 刷新视图
3. `generate_layer_thumbnail` 接受 `size` 参数，不再使用全局常量

## 状态
✅ 已修复
