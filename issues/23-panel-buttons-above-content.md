# Issue: 面板按钮移至内容上方

## 描述
图层面板和文件列表面板的按钮（大小预设、上下移动、添加/删除）位于内容
区域的底部，应移至标题行正下方，使按钮紧贴面板标题。

## 修复方案
将 `layer_panel.py` 和 `file_list.py` 中 `_setup_ui` 的布局顺序从
「内容 → 按钮行」改为「按钮行 → 内容」，并给内容控件设置 `stretch=1`
以占用剩余空间。

## 状态
✅ 已修复
