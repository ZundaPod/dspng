# Issue: GUI 图层列表顺序与 Photoshop 不一致

## 描述
图层列表按从下到上的顺序显示（index 0 = 最底层），但用户期望看到
从上到下的顺序（与 Photoshop 图层面板一致）。

渲染合成顺序不变（仍然从下到上），仅反转 GUI 显示顺序。

## 修复方案
在 `_populate_children` 中反转子节点顺序，使 wrapper 列表按从上到下
排列。底层数据模型不变。

## 状态
✅ 已修复
